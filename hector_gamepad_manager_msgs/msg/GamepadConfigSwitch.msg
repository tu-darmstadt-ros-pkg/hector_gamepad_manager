# A globally reserved button that switches the active configuration.
string name         # canonical input name of the button, e.g. "back"
string config       # config this button switches to
string description
