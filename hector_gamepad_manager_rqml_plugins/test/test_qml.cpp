// QuickTest entry point for the plugin's ROS-free QML logic. VirtualGamepadState.qml imports
// nothing but QtQuick, so these tests need no Ros2 mocks.
#include <QtQuickTest/quicktest.h>

QUICK_TEST_MAIN( virtual_gamepad )
